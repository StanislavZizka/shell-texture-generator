Legacy Python helpers kept for reference.

These scripts are not part of the active CLI surface.
They stay here so older ad hoc workflows remain available without cluttering the main `scripts/` directory.
